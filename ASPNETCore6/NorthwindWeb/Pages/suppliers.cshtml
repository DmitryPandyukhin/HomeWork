@page
@model NorthwindWeb.Pages.SuppliersModel
<div class="row">
    <h1 class="display-2">Поставщики</h1>
    <table class="table">
        <thead class="thead-inverse">
            <tr><th>Название организации</th></tr>
        </thead>
        <tbody>
            @foreach (string name in Model.Suppliers)
            {
                <tr><td>@name</td></tr>
            }
        </tbody>
    </table>
</div>